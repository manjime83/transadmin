{
  "name": "@transadmin/delete-note",
  "version": "1.0.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc",
    "prepack": "npm update --dev && npm install && npm run build",
    "postpack": "bash ../../scripts/postpack.sh",
    "deploy": "npm pack && bash ../../scripts/update-function-code.sh"
  },
  "dependencies": {
    "tslib": "^1.11.1",
    "uuid": "^7.0.2"
  },
  "bundleDependencies": [
    "tslib",
    "uuid"
  ],
  "devDependencies": {
    "@types/aws-lambda": "^8.10.46",
    "@types/node": "^13.9.1",
    "@types/uuid": "^7.0.2",
    "aws-sdk": "^2.641.0",
    "typescript": "^3.8.3"
  }
}
